import { PesquisaCategoriaPipe } from './pesquisa-categoria.pipe';

describe('PesquisaCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new PesquisaCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
