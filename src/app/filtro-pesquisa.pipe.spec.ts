import { FiltroPesquisaPipe } from './filtro-pesquisa.pipe';

describe('FiltroPesquisaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPesquisaPipe();
    expect(pipe).toBeTruthy();
  });
});
