import { PesquisaProdutoPipe } from './pesquisa-produto.pipe';

describe('PesquisaProdutoPipe', () => {
  it('create an instance', () => {
    const pipe = new PesquisaProdutoPipe();
    expect(pipe).toBeTruthy();
  });
});
